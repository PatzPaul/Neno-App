# Neno-App

Expo client for **Neno**, a Swahili-first Bible & SDA learning app. API: [Neno-API](https://github.com/PatzPaul/Neno-API).

```sh
npm install
npx expo start      # scan with Expo Go
```

Talks to `EXPO_PUBLIC_API_URL` (see `.env`).
